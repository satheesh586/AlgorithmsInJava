package in.LinkedList.Basic;

import in.LinkedList.ListNode;
import in.LinkedList.SingleLinkedList;

/**
 * Created by satheeesh on 11/8/16.
 */
public class MergeSortedLists {

   /* public ListNode mergeSortedLists(SingleLinkedList s1, SingleLinkedList s2) {
        ListNode firstList = s1.getHead(), secondList = s2.getHead(), newList = null;
        while (firstList != null && secondList != null) {
            if (newList == null) {
                if (firstList.getData() > secondList.getData()) {
                    newList = secondList;
                    secondList = secondList.getNext();
                }
                else {
                    newList = firstList;
                }
            }

        }
    }*/
}
